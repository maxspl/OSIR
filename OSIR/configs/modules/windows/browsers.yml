metadata:
  version: "2.0"
  author: maxspl
  description: Parsing of browsers artifact.
  os: windows

configuration:
  module: browsers
  type: process
  disk_only: false
  no_multithread: false
  processor_type:
  - external
  processor_os: unix

tool: 
  path: python
  cmd: /OSIR/OSIR/bin/BrowserParser/main.py {input_dir} {output_dir} --jsonl
  source: https://github.com/maxspl/BrowserParser
  version: 1.1.0

input:
  type: dir
  path: r"restore_fs\/[^\/]+\/C$"

output:
  type: multiple_files
  format: jsonl
  output_prefix: "{endpoint_name}--{module}-"

endpoint: 
  patterns:
    - r"restore_fs\/(.*?)\/"
  default: "UNKNOWN"

splunk:
  'application:firefox':
    name_rex: .*firefox.*\.jsonl$
    path_suffix: 'browsers'
    sourcetype: 'application:browser:firefox'
    host_rex: ([\w\.-]+)--
  'application:chrome':
    name_rex: .*chrome.*\.jsonl$
    path_suffix: 'browsers'
    sourcetype: 'application:browser:chrome'
    host_rex: ([\w\.-]+)--
  'application:edge':
    name_rex: .*edge.*\.jsonl$
    path_suffix: 'browsers'
    sourcetype: 'application:browser:edge'
    host_rex: ([\w\.-]+)--