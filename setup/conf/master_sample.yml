splunk:
  location: {splunk_location}
  user: {splunk_user}
  password: {splunk_password}
  port: {splunk_port}
  mport: {splunk_mport}
  ssl: {splunk_ssl}
  local_splunk:
    previous_data: erase # values: erase, keep, stop
  remote_splunk:
    host: {splunk_remote_splunk_host}
