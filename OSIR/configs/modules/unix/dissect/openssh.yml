metadata:
  version: "2.0"
  author: Typ
  description: Parse openssh authorized_keys,known_hosts,private_keys,public_keys from UAC [root] using Dissect plugin
  os: unix

configuration:
  module: openssh
  alt_module: dissect_dispatcher
  type: process
  disk_only: false
  no_multithread: false
  processor_type:
  - internal
  processor_os: unix

input:
  type: dir
  paths:
    - r"extract_uac.*\/\[root\]$"

output:
  type: single_file
  format: jsonl
  output_file: "{endpoint_name}--{module}.jsonl"

endpoint: 
  patterns:
    - r"extract_uac\/Endpoint_(.*?)\/"
  default: "UNKNOWN"

optional:
  plugin: openssh

splunk:
  'linux:openssh':
    name_rex: \.jsonl$
    sourcetype: 'linux:openssh'
    host_rex: ([\w\.-]+)--
    timestamp_path: 
        - "ts"
    timestamp_format: "%Y-%m-%dT%H:%M:%S.%f%z"
    artifact: "openssh"