version: 1.1
author: Kelly Brazil
module: dmidecode
description: Kelly Brazil - JsonConverter - Parsing the output of the command dmidecode
os: unix
type: process
requirements:
disk_only: false
no_multithread: false
processor_type: 
  - external
processor_os: unix
tool: 
  path: cat
  cmd: "{input_file} | jc --dmidecode | jq -c '.[]' > {output_file}"
input:
  type: file
  paths:
    - r"dmidecode\.txt$"
output:
  type: single_file
  format: jsonl
  output_dir: "{case_path}/live_response/hardware"
  output_file: "{endpoint_name}--{input_file}.jsonl"
endpoint: "extract_uac\/(.*?)\/"
splunk:
  'linux:live_response:dmidecode':
    name_rex: \.jsonl$
    path_suffix: dmidecode
    sourcetype: 'linux:live_response:dmidecode'
    host_rex: _([\w\.-]+)--
    artifact: dmidecode